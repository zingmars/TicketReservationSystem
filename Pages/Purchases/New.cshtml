@page
@model TicketReservationSystem.Pages.Purchases.NewModel

@{
    ViewData["Title"] = "Buy a ticket";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Pirkt biļeti priekš @Model.Performance.Name!</h1>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Purchases.PerformanceId" />
            <div class="form-group">
                <label class="control-label">Apmaksas metode</label>
                <select asp-for="Purchases.PurchaseMethodId" class ="form-control" asp-items="ViewBag.PurchaseMethodId"></select>
            </div>
            <div class="form-group">
                <label class="control-label">Datums</label>
                <select asp-for="Purchases.PerformanceDateId" class ="form-control" asp-items="ViewBag.PerformanceDateId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Pirkt" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <b>Šobrīd, dotā sistēma neatbalsta naudas tranzakcijas.</b>
</div>
<div>
    <a asp-page="Index">Atgriezties atpakaļ</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
